{
  "name": "ai-smart-expense-tracker-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace runner for AI Smart Expense Tracker MERN application",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "node run.js",
    "start": "node run.js",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix backend"
  }
}
